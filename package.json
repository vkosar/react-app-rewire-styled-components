{
  "name": "react-app-rewire-styled-components",
  "version": "3.0.3",
  "description": "A plugin to add the styled-components Babel plugin to create-react-app with react-app-rewired",
  "repository": {
    "type": "git",
    "url": "https://github.com/vkosar/react-app-rewire-styled-components.git"
  },
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "precommit": "lint-staged",
    "prebuild": "rimraf dist",
    "build": "babel src --out-dir dist --ignore '*.test.js'",
    "test": "echo No tests yet",
    "prettier": "prettier --write --single-quote --trailing-comma es5 'src/**/*.js'",
    "flow": "flow src"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write --single-quote --trailing-comma es5",
      "git add"
    ]
  },
  "dependencies": {
    "babel-plugin-styled-components": "^2.0.7",
    "react-app-rewired": "^2.2.1"
  },
  "peerDependencies": {
    "styled-components": ">=2"
  },
  "devDependencies": {
    "@babel/cli": "^7.18.10",
    "@babel/preset-env": "^7.18.10",
    "@babel/preset-flow": "^7.18.6",
    "flow-bin": "^0.45.0",
    "husky": "^0.13.4",
    "lint-staged": "^3.6.1",
    "prettier": "^1.3.1",
    "rimraf": "^2.6.2"
  }
}
